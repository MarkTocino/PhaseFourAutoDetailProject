README
To set up and clone this repo:
git clone
run bash setup.sh to install all required dependencies
run python server/app.py to run flask API
run npm install inside of an auto-shop-project directory
run npm run dev inside of auto-shop-project directory to run react frontend

Welcome!
This Application allows users to:
- Setup Appointments
- Buy Cars
- Sign Up or Login If Account has already been created

Technologies Used:
Front-end: Next.js
Back-end: Flask, Python SQL.

Credits: Sam Ou & Mark Tocino
